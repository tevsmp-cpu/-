## 2025-05-15 - [Accessible Metric Cards]
**Learning:** Interactive cards implemented as `div` elements prevent keyboard navigation. Converting them to `button` (or `motion.button`) with `type="button"` and `text-left` maintains layout while enabling accessibility.
**Action:** Always use semantic buttons for clickable items and ensure `focus-visible` rings are provided for keyboard users.
